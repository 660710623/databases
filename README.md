# 🎬 Movie Booking System (Text-based)

โปรเจกต์นี้เป็นระบบ **Movie Booking System แบบ Text-based**  
พัฒนาด้วยภาษา **Python** และเชื่อมต่อฐานข้อมูล **MySQL**  
รองรับการเพิ่ม แสดง และลบข้อมูล พร้อมการใช้งานคำสั่ง `SELECT JOIN` ระหว่างหลายตาราง

---

## 📌 Features
- เชื่อมต่อฐานข้อมูล MySQL
- เพิ่มข้อมูล (INSERT)
  - Movie
  - Customer
- แสดงข้อมูล (SELECT)
  - แสดงรายการ Movie
  - แสดงข้อมูล Booking ด้วย JOIN หลายตาราง
- ลบข้อมูล (DELETE)
  - ลบ Booking (มีการตรวจสอบก่อนลบ)
  - ลบ Movie (มีการตรวจสอบก่อนลบ)
- ระบบเมนูแบบ Text-based (Command Line)

---

## 🛠 Technologies Used
- Python 3
- MySQL
- mysql-connector-python
- Git / GitHub

---

## 🗂 Database Structure
ตารางที่ใช้ในระบบ:
- Movie
- Customer
- Ticket
- Cinema
- Booking

ความสัมพันธ์ของข้อมูลใช้ **Primary Key และ Foreign Key**  
โดยมีการ JOIN ระหว่าง Booking, Ticket, Cinema และ Movie

---

## 🔗 Database Connection
โปรแกรมเชื่อมต่อฐานข้อมูลผ่านฟังก์ชัน `connect_db()`  
เพื่อให้สามารถเรียกใช้งานซ้ำได้ในทุกฟังก์ชัน

```python
def connect_db():
    return mysql.connector.connect(
        host="134.209.101.105",
        user="group32",
        password="password32",
        database="db_group32"
    )
