🔹 ภาพรวมของโปรแกรม

โปรแกรมนี้เป็น Movie Booking System แบบ Text-based
พัฒนาด้วยภาษา Python และเชื่อมต่อฐานข้อมูล MySQL
สามารถทำงานหลักได้แก่

เพิ่มข้อมูล (INSERT)

แสดงข้อมูล (SELECT + JOIN)

ลบข้อมูล (DELETE)
โดยมีการเชื่อมโยงข้อมูลระหว่างหลายตารางตามโครงสร้างฐานข้อมูล

🔹 การเชื่อมต่อฐานข้อมูล
def connect_db():
    return mysql.connector.connect(
        host="134.209.101.105",
        user="group32",
        password="password32",
        database="db_group32"
    )


ฟังก์ชันนี้ใช้สำหรับเชื่อมต่อกับฐานข้อมูล MySQL
โดยกำหนดค่า host, username, password และ database
เพื่อให้ฟังก์ชันอื่น ๆ สามารถเรียกใช้งานการเชื่อมต่อฐานข้อมูลได้โดยไม่ต้องเขียนซ้ำ

🔹 ฟังก์ชันเพิ่มข้อมูล (INSERT)
1️⃣ เพิ่มข้อมูลภาพยนตร์
def insert_movie():


ฟังก์ชันนี้รับข้อมูลจากผู้ใช้ ได้แก่

MovieID

MovieName

MovieGenre

MovieLength

แล้วนำข้อมูลไปบันทึกลงตาราง Movie
มีการใช้ try-except เพื่อดักจับ error ที่อาจเกิดขึ้นจากฐานข้อมูล

2️⃣ เพิ่มข้อมูลลูกค้า
def insert_customer():


ฟังก์ชันนี้ใช้เพิ่มข้อมูลลงในตาราง Customer
ประกอบด้วย

CustomerID

Name

SeatNumber

🔹 ฟังก์ชันแสดงข้อมูล (SELECT)
แสดงรายการภาพยนตร์ทั้งหมด
def show_movies():


ฟังก์ชันนี้ใช้คำสั่ง SELECT
เพื่อดึงข้อมูลจากตาราง Movie
และแสดงผล MovieID, MovieName, MovieGenre และ MovieLength

แสดงข้อมูลการจอง (SELECT + JOIN)
def select_with_join():


ฟังก์ชันนี้ใช้คำสั่ง JOIN ระหว่างหลายตาราง ได้แก่

Booking

Ticket

Cinema

Movie

เพื่อแสดงรายละเอียดการจอง เช่น

BookingID

TicketID

CinemaID

MovieName

ซึ่งแสดงให้เห็นความสัมพันธ์ของข้อมูลในฐานข้อมูล

🔹 ฟังก์ชันลบข้อมูล (DELETE)
ลบข้อมูลการจอง
def delete_entry(booking_id):


ก่อนลบ จะตรวจสอบก่อนว่าข้อมูล BookingID นั้นมีอยู่จริงหรือไม่
ถ้ามีจึงทำการลบข้อมูล เพื่อป้องกันการลบข้อมูลที่ไม่มีอยู่

ลบข้อมูลภาพยนตร์
def delete_movie():


ฟังก์ชันนี้ใช้ลบข้อมูลจากตาราง Movie
โดยตรวจสอบ MovieID ก่อนทำการลบเช่นเดียวกัน

🔹 ส่วนควบคุมการทำงานหลัก (Main Menu)
if __name__ == "__main__":


โปรแกรมใช้โครงสร้างเมนูแบบวนลูป (while True)
ให้ผู้ใช้เลือกการทำงาน เช่น

เพิ่มภาพยนตร์

เพิ่มลูกค้า

แสดงภาพยนตร์

แสดงข้อมูลการจอง

ลบการจอง

ลบภาพยนตร์

ออกจากโปรแกรม

ช่วยให้ผู้ใช้สามารถใช้งานระบบได้ง่ายผ่าน Command Line
